## 1. 快捷键配置模型与读写

- [x] 1.1 新增 `HotkeyConfig` 类：定义三个动作（Toggle 显示/隐藏、PrevMap、NextMap），每动作含默认键列表（Toggle 默认 `F2,Oemtilde`，PrevMap 默认 `Left`，NextMap 默认 `Right`）与当前键列表，验证方式：编译通过后在代码评审中确认动作与默认值与 spec 一致
- [x] 1.2 实现 `Keys` 与配置字符串双向转换（`KeysConverter` + 特殊键友好显示名映射，如 `Oemtilde`→`` ` ``），验证方式：临时单测输出各特殊键转换结果正确
- [x] 1.3 实现 `Load()` 与 `Save()`：读写 exe 同目录 `hotkeys.ini`（`动作名=键名1,键名2` 格式），未知动作名/非法键名/缺失文件逐项回退默认值，验证方式：手工构造正常与损坏的 ini 文件，程序分别加载出预期键集
- [x] 1.4 实现 `RestoreDefaults()` 与「未自定义不落盘」语义（首次运行不生成 ini，保存时才写入），验证方式：首次启动后确认无 `hotkeys.ini` 生成，点保存后生成且内容正确

## 2. 设置窗口

- [x] 2.1 新增 `SettingsForm` 模态窗口：三个动作各一行（动作名 + 当前键显示 + 「修改」按钮），底部「恢复默认」「确定」「取消」按钮，验证方式：托盘菜单打开窗口，界面元素齐全、默认显示 `` ` ``（F2）/ `←` / `→`
- [x] 2.2 实现按键监听：点击「修改」进入监听态（该行提示「请按键…，Esc 取消」），`KeyPreview` + `KeyDown` 捕获下一次按键（含方向键），Escape 取消本次修改，监听态结束回显结果，验证方式：分别按字母、数字、方向键、Escape 各测一次监听行为
- [x] 2.3 实现保存校验：对各动作键集全局查重，冲突时 MessageBox 提示冲突动作与按键并拒绝保存（不写入、不关窗、各动作保持原绑定），验证方式：将两个动作设为同一键后点「确定」，确认提示且窗口仍打开、绑定未变
- [x] 2.4 实现「恢复默认」：一键还原三动作默认键并同步显示，验证方式：修改后点击恢复默认，界面回显示默认键

## 3. 与主程序集成

- [x] 3.1 托盘菜单新增「设置」项并打开 `SettingsForm`（模态），验证方式：右键托盘图标可打开设置窗口
- [x] 3.2 钩子改造：新增 `settingsOpen` 标志（设置窗口打开期间钩子对所有键仅 `CallNextHookEx`，不消费不触发），验证方式：设置窗口打开时按 `` ` ``/`←`/`→` 均不切换显隐或翻页
- [x] 3.3 钩子分发改造：删除硬编码 VK 常量，改为「保存后重建 按键→动作 映射，`HookCallback` 仅查映射」的分发逻辑，验证方式：修改显隐键为 `Q` 保存后，按 `Q` 切换、按 `` ` `` 无反应；修改地图键为 `W`/`E` 后按 `W`/`E` 翻页、`←`/`→` 无反应
- [x] 3.4 启动加载：`Main`/窗体构造中调用 `HotkeyConfig.Load()` 并以映射初始化钩子，验证方式：配置 `Toggle=Q` 后重启程序，按 `Q` 直接生效而无需进设置窗口

## 4. 编译与整体验收

- [x] 4.1 运行 README 中 csc 编译命令重新编译 `Crosshair.exe` 且无编译错误，验证方式：编译命令退出码 0，新 exe 可启动、托盘图标出现
- [x] 4.2 按 spec 全部 6 条 Requirement 的场景逐条手工验收（打开设置、修改生效、重启持久、恢复默认、冲突拒绝、旧键失效），验证方式：验收清单逐项勾选通过（含首次运行默认键、损坏 ini 回退两项额外检查）