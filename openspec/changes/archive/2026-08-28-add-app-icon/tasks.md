## 1. 图标生成与代码

- [x] 1.1 新增静态图标生成方法（统一红点绘制：透明背景 + 与托盘一致的 `(255,60,60)` 圆点），改造 `SetupTrayIcon` 与窗口构造复用该方法（托盘 16px、窗口 `this.Icon` 32px），验证方式：csc 编译通过；临时单测断言 32×32 渲染结果（中心像素为红点色、四角为透明）
- [x] 1.2 用同一生成逻辑产出 `pubg.ico`（32×32）入库，验证方式：用 .NET `Icon.Load`/`new Icon(path)` 可加载且尺寸为 32×32

## 2. 构建集成

- [x] 2.1 编译命令加入 `/win32icon:pubg.ico` 重新编译 `Crosshair.exe`，验证方式：编译退出码 0、exe 文件存在；资源管理器查看文件图标为红点（实机确认）
- [x] 2.2 同步更新 README 与 HANDOFF 中的编译命令（含 `/win32icon` 与图标生成说明），验证方式：文档包含新编译参数与图标重新生成方法

## 3. 编译与整体验收

- [x] 3.1 启动冒烟：运行新 exe 进程存活、托盘红点图标正常，验证方式：进程存活、托盘图标可见（实机）
- [x] 3.2 按 spec 场景手工验收（资源管理器 exe 图标为红点、运行窗口/任务栏图标为红点、与托盘一致），验证方式：验收清单逐项勾选（需实机环境）